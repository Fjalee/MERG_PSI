<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_UI.Views.HomePage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout HorizontalOptions="StartAndExpand" VerticalOptions="Start" Margin="10" >
                <Label Text="Nekilnojamasis turtas" TextColor="DarkGray" FontSize="20" FontAttributes="Bold"/>
            </StackLayout>


            <CollectionView x:Name="myItem" SelectionMode="Single" SelectionChanged="MyItem_SelectionChanged">
                <CollectionView.ItemTemplate>
                    <DataTemplate>


                        <StackLayout Margin="15" Padding="0">
                            <Grid>
                                <Frame BackgroundColor="White" HasShadow="True" Margin="0" HeightRequest="55" VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand">
                                    <StackLayout Margin="20,10,5,5" Padding="0">
                                        <Label Text="{Binding Municipality}" Margin="80,-5,0,0" TextColor="DarkGray" FontSize="16" FontAttributes="Bold"/>
                                        <Label Text="{Binding Microdistrict}" Margin="100,-10,-10,0" TextColor="DarkGray" FontSize="12" FontAttributes="Bold"/>
                                        <Label Text="{Binding Price}" Margin="100,-10,-10,0" TextColor="Green" FontSize="13" FontAttributes="Italic"/>

                                    </StackLayout>
                                </Frame>
                                <StackLayout Margin="15,-25,0,0">
                                    <Frame BackgroundColor="Blue" HasShadow="True" Margin="0" HeightRequest="90" WidthRequest="90" Padding="0" VerticalOptions="CenterAndExpand" HorizontalOptions="Start">
                                        <Image Source="{Binding Image}" Aspect="AspectFill"/>
                                    </Frame>
                                </StackLayout>
                            </Grid>
                        </StackLayout>


                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>